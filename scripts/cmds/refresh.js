module.exports = {
  config: {
    name: "refresh",
    version: "1.0.0",
    author: "Hridoy",
    countDown: 5,
    role: 0,
    shortDescription: "Giant Refresh Message",
    longDescription: "Send a giant frog-snake refresh message",
    category: "Admin",
    guide: "{pn}"
  },

  onStart: async function ({ message }) {
    const refreshMessage = `🐸































































































































































































































































































































































































































































































































































































































































































































































➖

































































































































































































































































































































































































































































































































































































































































































































































































































🐍`;

    try {
      return message.reply(refreshMessage);
    } catch (error) {
      console.error(error);
      return message.reply("❌ Failed to send refresh message!");
    }
  }
};